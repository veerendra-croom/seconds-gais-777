
import { Item, Category, UserProfile } from './types';

// NOTE: MOCK_USER, MOCK_ITEMS, COLLEGE_LIST, and COLLEGE_COORDINATES 
// have been removed to ensure the application relies solely on the 
// Supabase database for authentication, data retrieval, and configuration.

export const DEFAULT_COLLEGE_COORDS = { lat: 34.0522, lng: -118.2437 }; // Fallback (Los Angeles)
